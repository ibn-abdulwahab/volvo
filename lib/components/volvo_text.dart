import 'package:flutter/material.dart';

class VolvoText extends StatelessWidget {
  const VolvoText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      'VOLVO',
      style: TextStyle(
        fontFamily: 'Clarendon',
        fontSize: 12,
      ),
    );
  }
}
